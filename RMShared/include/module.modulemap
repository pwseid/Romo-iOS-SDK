module RMShared {
    umbrella header "RMShared.h"

    // Headers not imported by RMShared.h but still available via #import
    exclude header "NSString+createSHA512.h"
    exclude header "RMRingBuffer.h"
    exclude header "RMSharedDefines.h"
    exclude header "RMWebSocket.h"
    exclude header "UIDevice+Hardware.h"
    exclude header "UIDevice+Romo.h"
    exclude header "UIView+Additions.h"

    export *
}
